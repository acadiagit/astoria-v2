# Astoria v2 — Local development Caddy config (HTTP only, no domain)

:80 {
    handle /api/* {
        reverse_proxy backend:8000
    }

    handle {
        root * /srv
        try_files {path} /index.html
        file_server
    }
}
